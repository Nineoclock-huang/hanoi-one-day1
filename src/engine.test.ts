import{describe,expect,it}from"vitest";import{analyzeConversation,analyzeMessage,clerkReply,completedCount,emptyCriteria,mergeCriteria,normalizeVietnamese}from"./engine";
describe("越南语规则识别",()=>{it("忽略声调、大小写和多余空格",()=>{expect(normalizeVietnamese("  CÀ   PHÊ SỮA ĐÁ  ")).toBe("ca phe sua da")});it("识别完整点单信息",()=>{const result=mergeCriteria(emptyCriteria,analyzeMessage("Cho toi mot ly ca phe sua da it duong mang di"));expect(completedCount(result)).toBe(4);expect(result).toMatchObject({product:true,quantity:true,sugar:true,service:true,payment:false})});it("识别付款确认",()=>{expect(analyzeMessage("Tôi thanh toán bằng tiền mặt").payment).toBe(true)})});
describe("对话累计理解",()=>{it("一句中同时出现商品和少糖时不会再次询问糖量",()=>{const state=analyzeConversation(["Cho toi ca phe sua da va it duong"]);expect(state.product).toBe(true);expect(state.sugar).toBe(true);expect(clerkReply(state).zh).toBe("你想要几杯？")});it("补充数量后跳过已回答的糖量，继续问用餐方式",()=>{const state=analyzeConversation(["Cho toi ca phe sua da va it duong","Mot ly"]);expect(clerkReply(state).zh).toBe("堂食还是带走？")});it("理解少甜等常见同义说法",()=>{expect(analyzeMessage("ca phe sua da it ngot").sugar).toBe(true)})});
